# ============================================================
# 权限数据结构（纯展示用，无实际运行作用）
# ============================================================
#
# 设计思路：
#   权限按三个维度组合——页面、导航栏、行为。
#   每个权限等级在这三个维度上分别定义可访问/可操作的范围。
#   高等级可继承低等级的权限（动态可调）。
#
# 注意：
#   这里的结构只是概念占位，具体的前端适配逻辑和后端编排逻辑
#   尚未设计完成，后续再完善。
#
# ============================================================

permission_levels = {
    "PL_001": {
        "permission_id": "PL_001",
        "name": "",
        "rank": 0,
        # 三个维度的权限
        "pages": [],  # 可访问的页面列表
        "nav_items": [],  # 导航栏可见项列表
        "actions": [],  # 可执行的行为列表
    },
}
